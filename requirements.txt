fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
python-docx>=1.1.0
requests>=2.31.0
beautifulsoup4>=4.12.0
playwright>=1.40.0
apscheduler>=3.10.0
psycopg2-binary>=2.9.9
lxml>=5.0.0

# Day 1: 文档解析引擎
pdfplumber>=0.11.0

# OCR 引擎（按需安装，体积较大 ~500MB）
paddlepaddle>=3.3.0
paddleocr>=3.5.0

# Day 3: 文件上传安全
python-magic>=0.4.27    # MIME 类型检测（需要系统包 libmagic1）

# Day 4/5: PDF 导出（按需安装，至少一种）
# 方案 1（推荐，Docker/System 中安装 libreoffice）：
#   apt install libreoffice
# 方案 2（备选）：
# docx2pdf>=0.1.8
# weasyprint>=60.0
# python-mammoth>=0.6.0

# Day 5: 病毒扫描（可选）
# pyclamd>=0.4.0  # 需要系统安装 ClamAV: apt install clamav clamav-daemon
